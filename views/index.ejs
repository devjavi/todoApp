<% include ./layouts/header%>

    <% if(!currentUser) { %>
        <% include ./components/authForms%>
        
    <% } else if(!currentUser.isAdmin){%>
        <div><h1>Welcome <%=currentUser.fName%></h1>
        <div class="container-fluid"><%include ./components/profile%></div>
        <p>
            <%if(currentUser.todos.length > 0){%>
                Here you have your pending items:
            <%} else{%>
                You don't have pending items, you are awesome!
            <%};%>
        </p>
        </div>
        <div class="container"><% include ./components/todos%></div>
    <% }; %>


</body>
</html>
